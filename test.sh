# Check if you're in the right directory
pwd
ls -la

# Check if the project directories exist
ls -la eureka-server/
ls -la eureka-server/target/

# Test the find command manually
find eureka-server/target -name "*.jar"